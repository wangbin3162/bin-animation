@import "common.styl"

.animation-demo {
  .ctrl {
    padding-bottom: 20px;
  }
  .animation-box {
    width: 100%;
    height: 100px;
    .transition-box {
      width: 60px;
      height: 60px;
      border-radius: 4px;
      background-color: #409eff;
      text-align: center;
      color: #fff;
      line-height: 60px;
      box-sizing: border-box;
      font-size: 16px;
    }
  }
}
